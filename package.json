{
  "name": "reading_learning",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "frontend": "cd frontend && npm install && npm run dev",
    "backend": "cd backend && npm install && node server.js",
    "start": "npm run backend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mongodb": "^7.0.0"
  }
}
